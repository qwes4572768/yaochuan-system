# 資料庫（可遷移至 PostgreSQL）
# SQLite: sqlite+aiosqlite:///./hr.db
# PostgreSQL: postgresql+asyncpg://user:password@localhost:5432/hr
DATABASE_URL=sqlite+aiosqlite:///./hr.db

# 敏感欄位加密金鑰（Fernet，可由 Python: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode()) 產生）
# 未設定則不加密，僅 API/UI 遮罩
# ENCRYPTION_KEY=your_base64_fernet_key_here

# 人事備份/還原僅管理員可用：前端輸入此憑證後可執行備份與還原
# ADMIN_BACKUP_TOKEN=your_secret_admin_token

# 巡邏綁定 QR 對外公開網址（手機掃描需可連線）
PUBLIC_BASE_URL=https://your-domain.trycloudflare.com

# 每日自動備份：目錄（相對或絕對）、保留份數、每日執行時間 HH:MM
# BACKUP_DIR=server/backup/hr
# BACKUP_RETENTION_COUNT=30
# BACKUP_SCHEDULE_TIME=00:00
